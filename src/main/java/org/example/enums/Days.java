package org.example.enums;

public enum Days {
    BAZARERTESI(1),
    CERSENBEAXSAMI(2),
    CERSENBE(3),
    CUMEAXSAMI(4),
    CUME(5),
    SENBE(6),
    BAZAR(7);

    int index;

    Days(int index) {
        this.index = index;

    }
}
